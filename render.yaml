services:
  - type: web
    name: jobboard
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    # Render will build the Docker image using the Dockerfile; no explicit build/start commands required.
    envVars:
      - key: ASPNETCORE_URLS
        value: http://0.0.0.0:$PORT
